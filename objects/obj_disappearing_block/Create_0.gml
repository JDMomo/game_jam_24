// Create Event of obj_disappearing_block

// Initialize the timer variable
touchTimer = 0; // Timer to count down to disappearance
isTouched = false; // Flag to check if the block has been touched
solid = true; // Ensure the block is solid to act as ground
